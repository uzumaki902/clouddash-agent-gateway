# CloudDash Agent Gateway — Python dependencies
# Pin exact versions before deploying to production

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Pydantic (v2 required — v1 is NOT supported)
pydantic>=2.7.0

# LangChain + LangGraph
langchain>=0.2.0
langchain-openai>=0.1.6
langgraph>=0.1.0

# Supabase + pgvector
supabase>=2.4.0
vecs>=0.4.0               # Supabase pgvector Python client

# Config parsing
pyyaml>=6.0.1

# Observability
python-json-logger>=2.0.7

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.6
httpx>=0.27.0             # ASGI test client for FastAPI
